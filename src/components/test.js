const maxSeats = props.maxSeats // ask for the data from the flights.json - how many seats are on the plane

for (let i = 0; i <= maxSeats; i++) {
    const seatNumber = maxSeats[i];
    for (let i = 0; i < array.length; ++) {
    
        
    }

}